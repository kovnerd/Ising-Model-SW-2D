#include <gsl/gsl_rng.h>
#include <math.h>
#include <time.h>

void swendsen_wang_step(int **, int **, int ***, gsl_rng *);
int hk-cluster(int ***bond, int **clusterLabel);
int properLabel(int i, int *labelLabel)
void swendsen_wang_step_per_spin(int **, int**, int ***, gsl_rng *);
